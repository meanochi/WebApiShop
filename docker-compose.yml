version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: showscenter-redis
    command: redis-server --requirepass ShowsCenter123!
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  redis-data: